/*Crie uma função que recebe um número como parâmetro e retorna a sua representação em hexadecimal.*/

let hexadecimal = (numero)=>{
  
 
  return   numero.toString(16)
}

console.log(hexadecimal(1200));