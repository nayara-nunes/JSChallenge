/*Crie uma função que recebe um array de objetos e retorna um novo array apenas com os 
objetos que possuem uma determinada propriedade.

Crie uma função que recebe um array de objetos e retorna a soma dos valores 
de uma determinada propriedade em todos os objetos.

Crie uma função que recebe um objeto e retorna um novo objeto com as mesmas
 propriedades, mas com os valores em ordem alfabética.

Crie uma função que recebe um objeto e retorna um novo objeto com as mesmas propriedades,
 mas com os valores de todas as propriedades convertidos para maiúsculas.

Crie uma função que recebe um objeto e uma função de mapeamento e retorna um novo objeto 
com as propriedades transformadas pela função de mapeamento.

Crie uma função que recebe um array de objetos e retorna um novo objeto com as mesmas propriedades 
do primeiro objeto do array, mas com os valores sendo arrays que contém o valor da propriedade 
em cada objeto do array original.

Crie uma função que recebe um objeto e retorna um array com as propriedades que possuem valores 
que são funções.
Crie uma função que recebe um array de objetos e uma função de 
filtragem e retorna um novo array apenas com os objetos que passam no teste da função de filtragem.

Crie uma função que recebe um objeto e uma função de redução e 
retorna o resultado da redução das propriedades do objeto.

Crie uma função que recebe um array de objetos e retorna um novo array
 apenas com os objetos que possuem todas as propriedades especificadas.

Crie uma função que recebe um objeto e uma função de mapeamento e retorna um novo 
objeto com as propriedades transformadas pela função de mapeamento, mas apenas para as
 propriedades especificadas.

Crie uma função que recebe um array de objetos e retorna 
um objeto com o número de ocorrências de cada valor em uma propriedade específica.

Crie uma função que recebe um objeto e retorna um novo objeto
 com as mesmas propriedades, mas com os valores de todas as propriedades convertidos para números.

Crie uma função que recebe um array de objetos e retorna um novo 
array com os objetos em ordem alfabética de uma propriedade específica.

Crie uma função que recebe um objeto e uma função de filtragem 
e retorna um novo objeto com as propriedades que passam no teste da função de filtragem.

Crie uma função que recebe um array de objetos e retorna um novo array c
om as propriedades de todos os objetos combinados em um único objeto.

Crie uma função que recebe um objeto e retorna um novo objeto com todas as 
propriedades do objeto original, mas com os valores de uma propriedade específica duplicados.

Crie uma função que recebe um array de objetos e retorna um novo array com 
as propriedades de todos os objetos combinados em um único objeto, mas com os valores
 de cada propriedade sendo um array com os valores de todas as propriedades correspondentes 
 em cada objeto original.

Crie uma função que recebe um objeto e retorna um novo objeto 
com as mesmas propriedades, mas com os valores de todas as propriedades convertidos para strings.

Crie uma função que recebe um array de objetos e uma função de mapeamento 
e retorna um novo array com os resultados da função de mapeamento aplicação */