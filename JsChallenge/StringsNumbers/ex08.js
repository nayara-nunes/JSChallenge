
/*8. Escreva uma função que recebe um número como argumento. 
Se a for um número inteiro (sem casa decimal) retorna true. Caso contrário, retorna falso.*/
let inteiro = (number) =>{
   return Number.isInteger(number);
}
console.log(inteiro(23));

